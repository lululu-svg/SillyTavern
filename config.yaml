# 完全禁用安全检查（仅用于个人云部署）
security:
  ip_whitelist:
    enabled: false
  csrf:
    enabled: false

api:
  api_key_input: true
  backend_api_url: https://api.openai.com/v1
  default_chatbackend: chat

server:
  trust_proxy: true
  allowed_hosts: ["*"]
  port: 8000
